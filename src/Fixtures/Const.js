export const dateArray = Array(31)
  .fill(1)
  .map((eleme, index) => eleme + index);

  export const monthArray = Array(12).fill(1).map((eleme,index)=> eleme +index)

  export const yearArray = Array(44).fill(1980).map((eleme,index)=> eleme  +index)